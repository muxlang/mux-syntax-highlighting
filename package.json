{
  "name": "mux-syntax-highlighting",
  "version": "0.6.0",
  "private": true,
  "description": "Quality tooling for the Mux TextMate syntax definitions",
  "scripts": {
    "check": "npm run lint && npm run format:check && npm run samples && node scripts/check-parity.js && node scripts/build-editor-support.js --check",
    "format": "prettier --write 'eslint.config.js' 'scripts/check-samples.js' 'package.json' '.prettierrc.json'",
    "format:check": "prettier --check 'eslint.config.js' 'scripts/check-samples.js' 'package.json' '.prettierrc.json'",
    "lint": "eslint . --max-warnings 0",
    "samples": "node scripts/check-samples.js"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "prettier": "^3.6.2"
  }
}
